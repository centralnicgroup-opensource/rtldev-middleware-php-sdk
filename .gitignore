/tests/logs/*.txt
/tests/logs/*.log
basic-test.php
vendor
node_modules
build/api-cache
.phpunit.result.cache
.phpunit.cache
php-sdk.phar
php-sdk.phar.gz
reports
.pnpm-store
# do not remove the following line
# it blocks env.sh from being committed
# which contains sensitive data 
# (used for setting secrets for local testing)
env.sh

# generated API documentation — published to the gh-pages branch on release,
# regenerated locally via `composer docs` (served with `composer docs:serve`)
docs/

# uml diagram related
# (plantuml.jar now lives under tools/, covered below)
uml.puml
tools/
# guard against a stray plantuml.jar being re-added at the repo root
/plantuml.jar

# Claude Code — local user settings (personal overrides, MCP permissions)
.claude/settings.local.json