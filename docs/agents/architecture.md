# Architecture (deep dive)

Full architectural reference for the PHP SDK.

> **What this file is for.** It records what neither the code nor `git log` can: the alternative that was rejected and why, the failure mode each decision prevents, empirical results that are expensive to re-derive, and the condition under which a decision should be revisited. It is **not** a changelog — what a refactor changed belongs in [MIGRATION.md](../../MIGRATION.md) and the commit; it is **not** a class inventory — that is derivable from `src/` in a couple of greps, and a prose copy drifts while the code cannot. This file has twice had to correct its own claims about the code, both times in descriptive prose rather than in a decision.
>
> **Every record keeps to four fields, and anything that fits none of them is history and does not belong here:** a **directive** (imperative, one line), the **failure mode** it prevents, the **alternative rejected** plus any empirical result, and the **revisit condition** with the **guard test** filename. A record must not restate its guard's class docblock — CONTRIBUTING.md already requires that docblock to carry the directive, the failure mode and the revisit condition, so the record names the file and stops.
>
> CLAUDE.md carries a trip-wire pointing here, not a summary. A green suite is not evidence a decision still holds — most of these refactors are behaviour-preserving the day they land, which is why the guards are structural.
>
> `src/` docblocks and MIGRATION.md reference entries below **by name** ("the parse-seam entry", "the `ApiDateTime` entry"). Keep those identities when editing.

## Shape of the tree

`CNIC\` maps to `src/` (PSR-4), with `CNR`/`IBS`/`MONIKER` sub-namespaces. Read the inheritance chain and member lists off `src/`. The facts about the tree that are **decisions** rather than structure:

- **The brands are siblings, not parent/child.** `CNR\Response` and `IBS\Response` both extend `AbstractResponse`; neither is-a the other. IBS previously extended `CNR\Response` and threw `UnsupportedFeatureException` from 5 inherited CNR-only methods — a refused bequest. `MONIKER\Client extends IBS\Client` because they are the same API platform (only the `SocketConfig` differs), and is deliberately **not** `final` — see the session-seam entry. MONIKER has no `Response` of its own. (RSRMID-2904, RSRMID-2923.)
- **Response construction is a template method.** `AbstractResponse::__construct()` is the skeleton; brands supply `translate()`, `populate()`, `newRecord()` and `newResponseParser()`, each `#[\Override]`. **Never reimplement the constructor** — its shape is load-bearing, and the parse-seam entry below deliberately added a *sixth* dispatch rather than opening it up.
- **Placeholder stripping is per-field, not global.** `AbstractResponseTranslator` strips `{UPPER}` placeholders through a per-field callback confined to the human-readable field (`description` for CNR, `message` for IBS). **Failure mode:** a global strip corrupts `{UPPER}` content in real data fields. (RSRMID-2893.)

## Decision records

- **The pagination seam is drawn at the wire — do not hoist single-page defaults onto the base (RSRMID-2912 declined, RSRMID-2918 delivered, RSRMID-2943 narrowed, RSRMID-2965 relocated the derivation).** The 4 primitives (`getFirstRecordIndex`/`getLastRecordIndex`/`getRecordsTotalCount`/`getRecordsLimitation`) — the methods that read a brand's own pagination metadata — stay per brand, and every derivation from them is shared and written once. Since RSRMID-2965 the derivation lives on the `CNIC\Paginator` value object (`getCurrentPageNumber`/`getNextPageNumber`/`getNumberOfPages`/`getPreviousPageNumber`/`hasNextPage`/`hasPreviousPage`/`toArray`) rather than on the base, and `AbstractResponse` keeps only `getPagination()` (which constructs one from the 4 primitives plus `getRecordsCount()`) and `getRecordsCount()` itself. RSRMID-2912 was **declined and closed as Cancelled**; RSRMID-2918 delivered the original 7/5 split; RSRMID-2943 narrowed it to 4/8; this entry is the authoritative record for the current shape, not any of those tickets.
  - **What still stands from 2912/2918: do not hoist single-page DEFAULTS onto the base.** A brand must still be forced to answer what its own columns say for the 4 primitives — a base default would let a brand that forgot pagination silently report "page 1 of 1, no next page" instead of failing at declaration time. That half of the original decision is untouched by RSRMID-2943.
  - **What changed and why.** `getCurrentPageNumber()`/`hasNextPage()`/`hasPreviousPage()` read no wire column of their own — they were always pure functions of `getFirstRecordIndex()`/`getLastRecordIndex()`/`getRecordsLimitation()`/`getRecordsTotalCount()`, exactly like `getNextPageNumber()`/`getNumberOfPages()` already were. Pinning them as "primitives" protected nothing (there was no column read to force a brand to answer) and pinned a misclassification instead: CNR's own hand-rolled versions computed `hasNextPage()`/`hasPreviousPage()` from whole page numbers (`getCurrentPageNumber() ± 1` vs. `getNumberOfPages()`), while the sibling getters computed from record offsets — two arithmetic paths over the same four numbers that agree only when `FIRST` is a multiple of `LIMIT`. CNR's list API is offset-based (`FIRST`/`LAST`/`LIMIT`/`TOTAL`), so an unaligned window (e.g. `FIRST=50, LIMIT=100`) made the two paths disagree: the page-arithmetic predicate answered `hasNextPage()` from `getCurrentPageNumber()+1 <= getNumberOfPages()`, which said "true" for a window that already held the tail of the list, costing an empty follow-up request — and `hasPreviousPage()` claimed a mid-window position had no previous page (`getCurrentPageNumber()-1 > 0` needs a whole page's gap) when `FIRST=50 > 0` plainly does have preceding rows. Every predicate and page-number getter is now derived from the same offset grid (`FIRST`/`LAST` vs. `TOTAL`/`LIMIT`), so a predicate and its corresponding getter cannot disagree, aligned or not.
  - **An empty CNR window echoes `LAST = FIRST`, and that is what makes the offset walk terminate.** Three captured `QueryDomainList` shapes, all `count=0`: `FIRST=0, LIMIT=0` → `last=0, total=1825820`; `FIRST=2000000, LIMIT=0` → `last=2000000, total=1825824`; `FIRST=20000000, LIMIT=10` → `last=20000000, total=1825824`. `LAST` is **not** a flat floor of `0` — the first shape only looks like one because `FIRST` is `0` there too. Consequences: the third self-terminates on the arithmetic (`20000001 < 1825824` is false), while the first two need the **`LIMIT <= 0` gate**, because `LAST + 1 < TOTAL` holds and `requestNextResponsePage()` would otherwise advance to `FIRST = 1` and re-walk the list from near the start. That older guard is therefore still the load-bearing one; the offset grid did not replace it.
    - The second gate, `LAST < FIRST`, is **defensive only and no observed response triggers it** — it pins the invariant the advance rests on: since `LAST >= FIRST` always, `FIRST = LAST + 1` strictly increases and the walk is monotonic. A wire change breaking that would page *backward* rather than fail. Guarded by `CNR\ResponseTest::testWindowEndingBeforeItStartsHasNoNextPage`, whose comment says outright that the shape is synthetic.
    - **`getRecordsCount() === 0` is NOT a usable substitute for either gate**, however much "an empty window has no next page" sounds like the same statement. Until RSRMID-2965 the reason was mechanical — `assembleRecords()` sized the record list across *every* column, metadata included, so an empty window reported **one** record — and that is now fixed: an empty window honestly reports 0. The reason it still is not the gate is semantic: the row count describes the rows in hand, not whether more exist server-side, so a server answering an empty window mid-list would terminate a walk that should have continued. The `LIMIT <= 0` gate refuses only what was actually requested.
    - `LAST` is not a valid *request* parameter either — `FIRST`/`LIMIT` are the whole paging vocabulary — which is why `requestNextResponsePage()` throws `PaginationException` when a caller puts `LAST` in the command. Captures pinned in `CNR\ResponseTest::testPastTheEndWindow*` and `CNR\ClientTest::testRequestNextResponsePagePastTheEnd`.
  - **Rejected:** base defaults for the 4 primitives (as before); a `SinglePageResponse` trait (the `CNR\SessionCapable` precedent it leaned on is itself gone, RSRMID-2969) — with only two Response implementations, a default *or* trait would serve exactly one consumer.
  - Note the 4 are plain `ResponseInterface` methods the base never implements, **not** members `AbstractResponse` declares `abstract`. Reflection lists them as abstract members of `AbstractResponse` while `getDeclaringClass()` points at `ResponseInterface` — the misreading that produced the 2912 proposal. The status/code accessors are in the same category; what is specific to pagination is only the no-defaults rule. (`addColumn` used to be too, until RSRMID-2939 took it off the interface and made it a plain `protected` brand hook.)
  - **Revisit** if a brand's "more results" signal becomes a cursor or opaque token rather than a record offset — for such a brand `hasNextPage()` genuinely becomes a wire read again and belongs back in the primitive set. **Guard:** `tests/ResponsePaginationSeamTest.php`.
- **Response metadata is not column data (RSRMID-2965, breaking, v33.0.0).** A key matching a brand's `$metaKeys` (renamed from `$paginationKeys`) is never registered as a column: CNR's `TOTAL`/`FIRST`/`LAST`/`COUNT`/`LIMIT` and IBS's `transactid`/`status`/`message`/`code`/`total_*`/`domaincount` reach consumers through the pagination and status accessors only. The 4 primitives read `$hash` instead of `getColumn()`, `getColumnKeys(bool $filterPaginationKeys)` is retired, and `CNIC\Paginator` takes the derivation (see the entry above).
  - **The defect was a modelling one, and it cost four things.** A one-cell `TOTAL` column sat beside a 200-cell `DOMAIN` column while `assembleRecords()` sized rows as the max over every column — so metadata sized rows. That produced (1) a phantom record whose entire content was metadata for any empty window, (2) uneven row shapes on IBS (`status` on row 0 and no other row), (3) a `bool` flag on a public interface method plus a per-brand regex and a per-call `preg_grep` to undo the mixing, and (4) a documented hazard note warning `hasNextPage()` off the record count. **`assembleRecords()` needed no change at all** — its max-over-all-columns logic was correct once the column set was.
  - **The stand-ins went with it (extending RSRMID-2943's principle to the last six).** CNR's `getFirstRecordIndex()`/`getLastRecordIndex()` had `getRecordsCount()` fallbacks and IBS's `getRecordsTotalCount()`/`getRecordsLimitation()` literally returned it, so the "4 wire primitives" story the guard claimed to protect was already untrue. Absent metadata is now `null` everywhere, and the implicit-single-page rule in `getNumberOfPages()` covers the consequences: an IBS list with a count key is 1 page, an empty one 0 pages, an endpoint with no count key at all 1 page with its rows intact.
  - **`getRecordsCount()` deliberately stays `count($this->records)`.** It is `getRecord()`'s bounds authority; trusting a wire count there would let `getRecord()` pass its check and read an unset index (an undefined-array-key warning, which fails the suite since RSRMID-2964). The wire `COUNT`'s information reaches `Paginator` as its 5th member, fed by the record count — the two agree on every honest list, and the bounds-authority reading is the one that cannot be made to lie by a wire that miscounts. **This is a deliberate reading of decision 6 of the ticket**, which called the wire `COUNT` "CNR's fifth primitive": exposing it as a *fifth interface method* was rejected — it would be redundant with the record count for every honest list, contradict the 29 → 23 method accounting, and give `toArray()["COUNT"]` a `null` where `getListHash()` consumers have always had an int.
  - **On IBS `$metaKeys` is also a lookup pattern, not just an exclusion list.** The count key has four endpoint-dependent names (`domaincount`, `total_rules`, `total_records`, `total_hosts`), so the primitives scan the hash for the first key matching the count alternation, which is why that alternation is a separate constant (`COUNT_KEYS`) composed into `$metaKeys` rather than the other way around — matching `$metaKeys` would find `transactid` first. `totaldomains` stays **excluded**: `/Domain/Count` is a portfolio-structure query, not a list, and `.discount` is a real gTLD, so the anchoring is what keeps a data key named `discount` out of the metadata set.
  - **The two brands' metadata sets must stay independent.** Harmonising them would delete real data: CNR's `QueryDomainHistoryList` returns a data column named `STATUS`, which IBS's set treats as the transaction status.
  - **Rejected:** filtering metadata out at `assembleRecords()` (leaves the modelling error and the `bool` flag in place); keeping `getColumnKeys(bool)` as a deprecated no-op (a parameter that no longer does anything is worse than one that is gone); handling IBS's indexed-middle plain-text key names (`product_0_status`) — `IBS\Client` forces `ResponseFormat=JSON`, where those nest into sub-objects and never reach the SDK.
  - **Out of scope, same class of defect:** IBS transaction-level scalars (`currency`, `price`, `pricelevel`) still land on row 0 of a product response. That is an IBS row-model question, not a pagination one.
  - **Revisit** if a brand emits a genuine data column under one of its own reserved metadata names — which is an API defect rather than an SDK case, and would need the wire capture to argue from. **Guard:** `tests/ResponsePaginationSeamTest.php` (amended: the load-bearing half is now the negative on the brands, since re-pointing the ownership assertion at a `final` `Paginator` would be vacuous), plus `tests/PaginatorTest.php` for the arithmetic and `tests/ResponseInterfaceConsumerTest.php` for the consumer-facing rule.
- **A `Response` is sealed once constructed, and its records are iterated rather than stepped (RSRMID-2939, breaking, v31.0.0).** `ResponseInterface` lost six methods: the mutators `addColumn()`/`addRecord()` (now `protected`) and the record cursor `getCurrentRecord()`/`getNextRecord()`/`getPreviousRecord()`/`rewindRecordList()` (gone, with the three `protected` `has*Record()` predicates and the `$recordIndex` property). The interface `extends \IteratorAggregate<int, RecordInterface>`; consumers `foreach`, or use `getRecord(int)`/`getRecords()`.
  - **Five undeclared rules, all now type-enforced or impossible.** (1) Constructor step order was load-bearing and enforced only by a comment — `populate()` read `$this->command`/`$this->parser`/`$this->raw`, and moving the `$this->command` assignment below the call silently switched the IBS parser's wire branch. It now takes `populate(string $raw, ResponseParserInterface $parser, array $cmd): void`, so there is no order to get wrong and no `$parser` property at all. (2) A column added post-construction was absent from every record (records are assembled from the columns once, at the end of `populate()`). (3) `assembleRecords()` appended, so it was not idempotent; it now replaces. (4) A duplicate column name half-registered via `??=`, leaving `getColumns()` holding a column `getColumn()` could never return; `registerColumn()` now throws `DuplicateColumnException`. (5) The only way to query the cursor was to move it — the predicates were `protected` while `getNextRecord()` advanced shared state, so two consumers holding one response interfered.
  - **`getCurrentRecord()` went too, though the ticket only listed five methods.** With nothing able to move `$recordIndex`, it would have been a permanently frozen alias for `getRecord(0)` — a name that lies about what it does. Removing it makes the interface shrink by six, not five.
  - **The `DuplicateColumnException` throw is deliberately unreachable today** — not dead code by oversight. Both brands derive column names from `array_keys()` of the parsed hash, and two distinct PHP array keys cannot stringify to one name, so *not even a substitute parser* can reach it. It guards the three-list invariant against a future brand whose `populate()` builds columns some other way. Same policy as `CNR\Response::stringCells()`.
  - **Rejected:** keeping the cursor alongside `IteratorAggregate` (leaves the shared-state defect in place and gives two iteration idioms); making the response `Countable` (`getRecordsCount()` already exists, and `count($r)` would be ambiguous against `getRecordsTotalCount()`).
  - **Note for consumers, worth not re-deriving:** the old `getNextRecord()` pre-incremented, so a `while ($rec = $r->getNextRecord())` loop never saw record 0. A `foreach` ported from one legitimately sees **one more row**. Flagged in MIGRATION.md.
  - **Revisit** only on a concrete need to build a response incrementally from outside the brand's `populate()` — which does not exist today: responses are born from `AbstractClient::newResponse()`/`AbstractResponseTemplateManager::createResponseFromTemplateId()`, each handing a whole raw payload to a constructor, and a substitute `ResponseParserInterface` already controls what a response contains without any mutator. **Guard:** `tests/ResponseSealSeamTest.php`.
- **Records use an output factory hook, columns hand a finished instance to a shared registrar — do not "symmetrise" them (RSRMID-2899, RSRMID-2923).** `AbstractResponse::addRecord()` delegates to abstract `newRecord(array $row): RecordInterface`. Columns cannot use that shape, so each brand's `addColumn()` builds its own correctly-typed Column and passes it to `AbstractResponse::registerColumn(ColumnInterface $col): static`, which owns the `$columns`/`$columnKeys`/`$columnIndex` bookkeeping once. Keep the `registerColumn()` shape. Both `addRecord()` and `addColumn()` are `protected` since RSRMID-2939 — the seam shape is unchanged, only its visibility.
  - **Rejected, and infeasible rather than merely unattractive:** a param-typed `newColumn(string, array): ColumnInterface`. A CNR column took `string[]` and an IBS column mixed JSON values, so the base factory would have had to narrow `array<array-key,mixed>` into CNR's `string`-bound constructor — rejected by both PHPStan L9 (`argument.type`) and Psalm L1 (`MixedArgumentTypeCoercion`), and the toolchain forbids silencing either. Sharing a column *base* was a different question from a param-typed *factory*.
  - **⚠ The premise changed under RSRMID-2942 (v32.0.0) — re-derive before citing this.** That ticket deleted `CNR\Column`, so both brands now construct `CNIC\Column` from `array<array-key, mixed>` and the constructor divergence the infeasibility argument rested on no longer exists. The directive still stands in CLAUDE.md because nobody has re-tested it, **not** because the original reasoning still holds. Anyone re-opening it must re-run the analysers rather than quoting the paragraph above; anyone leaving it closed should record a current reason here.
  - **No guard test** — this one was enforced by the analysers, and is listed in CLAUDE.md for that reason.
- **The record/column layer is one class each — do not re-split it per brand (RSRMID-2923, breaking, v25.0.0).** One concrete `CNIC\Record` and one concrete `CNIC\Column`, used directly by every brand. `CNR\Column` survived this collapse as the one brand column, binding `TValue` to `string`; RSRMID-2942 below removed it too, so the rule is now simply **no brand declares a Record or a Column**.
  - **`newRecord()` stays an abstract per-brand hook** even though both brands return the same class: it is the seam a brand with genuinely different row behaviour would implement, and hard-coding `new Record()` on the base would close it. A hook body *is* the brand's declaration of its record type — unlike the empty `class Record extends AbstractRecord {}` markers this deleted, which carried no decision.
  - **Empirical, and worth not re-deriving:** a covariant `?string` override against the base's `mixed` return is accepted by **both** PHPStan L9 and Psalm L1 with no suppression, and `@extends CNIC\Column<string>` is genuinely enforced — `new CNR\Column("X", [1, 2, 3])` was rejected by both. All of that was true and none of it was the problem: see RSRMID-2942 below for why an *enforced* binding still reached no consumer.
  - **Guard:** `tests/RecordColumnSeamTest.php`.
- **A value-type narrowing belongs in a native return type on the interface, never in a generic (RSRMID-2942, breaking, v32.0.0).** `CNR\Column` bound `TValue` to `string` and narrowed `getDataByIndex(): ?string`, but `ColumnInterface` is not generic and **every** reachable path returns the interface — `getColumn(): ?ColumnInterface` and `getColumns(): ColumnInterface[]`, both declared on `ResponseInterface`. The binding was erased at the boundary, and this project's own "type-hint against interfaces" rule guaranteed no consumer ever held the narrowed type. `CNR\Column` and `@template TValue` are gone; `ColumnInterface::getStringByIndex(): ?string` and `RecordInterface::getStringByKey(): ?string` replace them.
  - **The measure of a public narrowing is what an integrator gets without configuring anything.** A docblock generic means nothing unless the integrator runs PHPStan or Psalm themselves; it is invisible in a plain IDE and at runtime. The repo proved the erasure twice before the fix — `examples/datetime.php` needed a hand-written `@var Column<string>`, and `CNR\Response::columnInt()` re-narrowed with `is_scalar()` at runtime precisely because the static type was gone.
  - **Making `ColumnInterface` generic was the rejected alternative, and it is worse than it sounds.** The erasure is at `getColumn()`, so `ColumnInterface<TValue>` alone fixes nothing — `ResponseInterface` would have to become generic too, pushing `ResponseInterface<string>` into every integrator's own type hints. That trades one annotation for a heavier one.
  - **Same shape as the date/time accessors (RSRMID-2318/2926), on purpose** — one-line, opt-in, declared on both the class and its interface, `null` rather than a throw for a value of the wrong type. Raw access stays on `getDataByIndex(): mixed` / `getDataByKey(): mixed`.
  - **`CNR\Response::stringCells()` was proposed for deletion and deliberately kept.** The ticket argued it existed only to satisfy the string binding. That was half right: the binding is gone, but the check independently makes a substitute parser's contract violation fail loudly, and keeps CNR cells string-guaranteed so `getStringByIndex()` cannot answer null-for-wrong-type on this brand. Deleting it would have weakened the very accessor this ticket added. Full reasoning under RSRMID-2924 above, where the check lives. **The dedicated type now exists:** `MalformedResponseException` (RSRMID-2967) is what the check throws instead of the bare `UnsupportedFeatureException` — documented as "capability absent on this platform" — it used to borrow for a malformed cell. It extends `UnsupportedFeatureException` rather than `CnicException` so the split stayed additive: both throw sites already raised the parent, and re-parenting them would have broken an existing `catch (UnsupportedFeatureException)` there.
  - **Guard:** `tests/RecordColumnSeamTest.php`, extended from "only CNR declares a Column" to "no brand declares one", plus a structural check that the two interfaces declare the narrowing accessors. Both halves are structural by necessity: re-adding a brand column, or demoting the accessor back to a generic, is behaviour-preserving on the day it lands.
- **The parse step is a seam, not a static call (RSRMID-2924, breaking, v26.0.0).** `CNIC\ResponseParserInterface` declares one signature, `parse(string $raw, array $cmd = []): array`, and both brand parsers are instantiable classes implementing it. Previously `populate()` called `RP::parse()` statically with **different signatures** per brand, so no shared contract was even expressible and nothing in the Response tree could be exercised without a full raw wire payload.
  - **The shape is the `TransportInterface` one, deliberately:** an abstract `newResponseParser()` hook supplies the brand default, and an optional trailing `?ResponseParserInterface $parser = null` constructor argument overrides it. Adding an optional parameter is legal widening, so no implementation stops satisfying anything.
  - **CNR takes `$cmd` and ignores it, on purpose.** A contract covering only IBS's shape, or two contracts, buys nothing — CNR's wire format is self-describing, and a uniform signature is what lets one seam serve both brands.
  - **`AbstractResponseTemplateManager::parseResponse()` is gone**, replaced by the same `newResponseParser()`. It still parses with **no** command (a template is not tied to one) while `populate()` passes the command it was built with; for IBS that selects the JSON versus plain-text branch. **Trap when asserting on that divergence:** the command must be one *without* `ResponseFormat` — one carrying `ResponseFormat=JSON` takes the same branch as the empty one and makes the assertion vacuous.
  - **A non-string cell — and equally a column entry that is not a list — throws** `MalformedResponseException` naming the column, from `CNR\Response::stringCells()` (it was the parent `UnsupportedFeatureException` until RSRMID-2967 — see the note above). Skipping would be the silent no-op RSRMID-2919/RSRMID-2920 ruled out; coercing would invent a value the wire could never carry. The container half is the one that is easy to forget: `foreach ((array)$values …)` turns a bare string into a one-cell column while a bare `int` still throws a line later. Deliberately asymmetric with the level above: a **missing or non-array `PROPERTY` block** yields no columns rather than throwing, because most CNR responses legitimately have none.
  - **RSRMID-2942 proposed deleting this and it was kept — the reasons are no longer the original ones.** The check was introduced to back `CNR\Column`'s `string` binding, and that class is gone. It stays because two independent reasons survived: only a *substitute* parser can reach it, so a violation is a programming error that should fail at construction rather than as a null three calls later; and CNR cells being string-guaranteed is exactly what stops `getStringByIndex()` answering null-for-wrong-type on this brand — an accessor is only as self-explaining as the data behind it. **Do not re-raise the deletion on the "it only serves the generic" argument; that argument is spent.** The former complaint — the borrowed exception type — was fixed additively without touching the check: see the `MalformedResponseException` note above.
  - **Guard:** `tests/ResponseParserSeamTest::testCNRRejectsANonStringColumnCellFromASubstituteParser()` and `::testCNRRejectsAColumnThatIsNotAListAtAll()`. Their inline comments still cite the `string` binding as the premise — read this entry, not those comments, for why the checks stand.
  - **`resetTemplates()` was `addTemplate()`'s counterpart, not a general undo — and it is gone.** It restored what the container held the first time `addTemplate()` ran for a class, so it was a no-op when the class was never added to, and a direct assignment to the public `$templates` property escaped it entirely: the RSRMID-2921 lesson that a guard on one writer does not protect state with several. The separate change it pointed to is RSRMID-2941 below, which removed the second writer by removing the shared container.
  - **Build-then-populate remains impossible by design.** The seam **adds a sixth** constructor dispatch rather than removing any, because injection makes the parse step substitutable without touching the constructor's load-bearing shape. **Revisit** as a ticket about the constructor, not about parsing.
  - **Guard:** `tests/ResponseParserSeamTest.php` (behavioural half via `CNICTEST\Support\SpyResponseParser`, structural half against a re-inlined `new RP()`).
- **The logger seam is at the format, not the sink (RSRMID-2925, breaking, v27.0.0).** `LoggerInterface::format(string $post, ResponseInterface $response, ?string $error = null): string` owns what varies per brand; `CNIC\LogSinkInterface::write(string): void` owns the destination. `CNIC\AbstractLogger` holds the sink (default `CNIC\EchoSink`), implements `log()` as `sink->write(format(…))` and leaves `format()` abstract. Previously the seam sat where nothing varied — every brand ended in `echo` — while the part that does vary returned nothing and could only be observed by capturing output.
  - **`AbstractLogger::log()` is `final`, and that is the load-bearing part.** A subclass reintroducing `log()` is behaviour-identical under the default echo sink and silently ignores an injected one. `final` turns that from an invisible regression into a declaration-time fatal.
  - **`setDefaultLogger()` was replaced by the protected `newLogger(LogSinkInterface $sink)` hook** plus public `AbstractClient::setLogSink()`. Without the hook, choosing a destination would mean `setCustomLogger(new CNIC\CNR\Logger($sink))` — naming a brand concrete at the call site, which is exactly what this project tells integrators not to do. `setLogSink()` rebuilds the brand logger and therefore **discards a previously set custom logger**; the two seams deliberately do not compose (a `LoggerInterface` has no sink slot), so last call wins and the order is pinned in both directions.
  - **Rejected:** a second logger interface instead of a sink. Two real implementations justify the sink immediately (`EchoSink`, `CNICTEST\Support\CollectingSink`), and it gives the WHMCS/Blesta bundles the brand's format for free. Note what the sink deliberately does **not** get: the response. A PSR-3 adapter deriving severity from the response status writes a `LoggerInterface`, not a sink — handing the response to both halves would make the split meaningless.
  - **Masking is unaffected** — both inputs are masked upstream (`sanitizeCommand()`, and `performRequest()` passing `getPOSTData($mycmd, true)`). **Trap:** assert on a `$post` built through the real client, never a hand-written pre-masked literal — the IBS format never renders `getCommand()`, so the POST body is its only secret-bearing input, and a literal passes with masking switched off entirely.
  - **Guard:** `tests/LoggerSeamTest.php`.
- **Core vs. extended Response contract — do not re-add the 5 extended methods to the core interface (RSRMID-2904).** `ResponseInterface` is the **universal** contract every brand fully supports. The richer CNR-class capabilities — telemetry (`getQueuetime()`/`getRuntime()`), transient/pending status (`isTmpError()`/`isPending()`) and `getListHash()` — live on `ExtendedResponseInterface extends ResponseInterface`, implemented **only** by `CNR\Response`. On IBS/Moniker those 5 are **absent**, not present-and-throwing. `AbstractClient::request()` returns the core interface, so a consumer holding the shared type narrows via `instanceof \CNIC\ExtendedResponseInterface`; consumers holding `CNR\Client`/`CNR\Response` are unaffected because `CNR\Client::request()` returns the concrete type.
- **`IBS\Response::getStatus()` was removed rather than promoted to a capability interface (RSRMID-2927, breaking, v28.0.0).** It returned the raw `status` hash key with zero callers in `src/` — every internal consumer already reads that key through protected `getHashString()`, and external consumers can reach it through the universal interface via `getHash()["status"]`.
  - **The judgment worth reusing:** a single method that only duplicates an already-interface-reachable hash lookup does not justify a new one-method capability interface plus the `instanceof` ceremony — unlike the CNR-only 5-method cluster that did justify `ExtendedResponseInterface`. This was a plain unused-API removal, not a repeat of the absence-vs-throw-vs-no-op policy question.
  - **Deliberately not guarded specifically.** Nothing pins "this method must stay gone"; a re-added public method not on any interface is caught by the sweep below, which is stronger than a one-off regression test naming `getStatus()`. Consumer path: [MIGRATION.md → v28.0.0](../../MIGRATION.md#-v2800), including the one case (`Domain/Check`'s `AVAILABLE`/`UNAVAILABLE`) where reading that key is the only way to get the answer — `isError()`/`isSuccess()` report only whether the *command* succeeded.
- **A sweep guards that every public member — method or property — is reachable through the interfaces CLAUDE.md mandates typing against (RSRMID-2927; properties added by RSRMID-2971).** `tests/InterfaceCoverageSeamTest.php` derives each concrete class's FQCN from its PSR-4 path and, for those implementing a "total" interface (the ones meant to fully describe their implementors — `ResponseInterface`, `RecordInterface`, `ColumnInterface`, `TransportInterface`, `ResponseParserInterface`, `LoggerInterface`, `LogSinkInterface`; **RSRMID-2941 added an eighth, `ResponseTemplateManagerInterface` — see below, and read `TOTAL_INTERFACES` rather than counting this sentence**), checks every public method for two defect shapes: **stray** (on none of them — the `getStatus()` shape) and **widening** (more parameters than the interface declares — the `getColumnKeys(bool)` defect fixed in `4b3ff7b`).
  - **RSRMID-2971 widened it to public properties, as a third shape and a second test method.** It enumerated `getMethods()` only, so the guarantee it advertised — every public member is interface-reachable — was being delivered for half the members, and the gap was occupied: `Column::$length` was a `public readonly int` no `ColumnInterface` consumer could reach. **The property check is absolute, not a comparison**, and that asymmetry is the point: a PHP 8.3 interface cannot declare a property at all, so unlike a stray method — reachable the moment someone adds it to the interface — a public property has *no* declaration that could fix it. A concrete class implementing a total contract may expose none. `ReflectionProperty::IS_PUBLIC` catches static and inherited ones too, which is what would now refuse the `public static array $templates` shape RSRMID-2941 removed by hand.
  - **`Column::$length` became `ColumnInterface::getLength()` rather than being dropped or allow-listed (RSRMID-2971, breaking, v33.0.0).** Three routes were on the table. *Allow-list it* was refused outright by the ticket and by the guard's own standing rule — an exception list turns a guarantee into an inventory of where it does not hold. *Delete it* and let consumers write `count($col->getData())` loses a real capability to save one method. *Accessor on the interface* keeps the capability and is breaking only for direct `ColumnInterface` implementers, of which the SDK has none (every column is built by a brand's `addColumn()`, naming the concrete class) — and v33 was already a major. The property stayed as `private readonly`, still backing `hasDataIndex()`. Note the deliberate asymmetry with `RecordInterface`, which gets no `getLength()`: a record is a keyed row, so a cell count answers nothing a caller asked.
  - **The interface-free value objects are outside the subject set, and that was decided rather than overlooked.** `ApiDateTime` keeps five public readonly properties and `Paginator` exposes none; neither implements an interface, so neither makes the reachability promise the sweep enforces — a consumer names those types directly because there is nothing else to name. Widening discovery to *every* class under `src/` would report `ApiDateTime`'s properties as defects when they are its entire designed surface. (This is the question RSRMID-2965 was sequenced before RSRMID-2971 to have answered once, with `Paginator` already in the tree.)
  - **Non-vacuity proven by mutation, twice.** A `public int` added to `CNIC\Record`, and `Column::$length` reverted to public — each failed the property sweep alone, with the method sweep green in both, so the new check is demonstrably what catches them. The revisit condition is narrow: PHP 8.4 lets an interface declare a property, at which point the absolute refusal could become a comparison like the method one — but the source language ceiling is pinned at 8.3 for as long as WHMCS ships ionCube-encoded, so it is not near-term.
  - **`ExtendedResponseInterface`/`RoleCredentialsInterface` are excluded from the "total" role** because they are additive capability interfaces, not descriptions of everything their implementor exposes; treating either as total would flag `CNR\Client`'s ~20 legitimate methods as stray. They still count as declaring interfaces, so `CNR\Response`'s 5 extended methods resolve rather than being reported themselves.
  - **The allow-list is empty and stays that way.** A future stray or widened method is a defect to fix at the source, not a reason to grow an exception list.
  - **`tests/ResponseInterfaceConsumerTest.php` was deliberately kept, not folded in.** The sweep is a *mechanism* check; the consumer test additionally pins *behaviour* through the interface and the no-constructor rule. Replacing one with the other drops a behavioural pin. Its subject moved in RSRMID-2965 — the capability it used to pin, `getColumnKeys(true)`, is gone with the modelling error behind it, so it now pins the replacement rule (metadata is unreachable through the column accessors, and pagination is reachable through the primitives) from the same interface-typed seat.
- **An interface declaration must match its implementation, and must not describe construction (RSRMID-2918, RSRMID-2923).** Two drifts on `ResponseInterface` resolved in *opposite* directions, which is the instructive part.
  - **`getColumnKeys()` — widen the interface to match the implementation.** The interface declared no parameter while `AbstractResponse` had always implemented `getColumnKeys(bool $filterPaginationKeys = false)`. Because this project mandates typing against the interface, the capability was **unreachable to exactly the consumers told to depend on it** — the call ran, but the analysers rejected it, leaving only bad workarounds. **General rule: when a parameter is added to an implemented method, add it to the interface in the same change.** That is breaking for direct implementers. (RSRMID-2965 then removed the parameter from both: it existed only because metadata was mixed into the column pool. The rule is unchanged — the example is now historical, so do not go looking for the `bool`.)
  - **`__construct()` — delete it from the interface instead.** Nothing constructs through these types: responses come from `AbstractClient::newResponse()`/`AbstractResponseTemplateManager::createResponseFromTemplateId()`, columns from a brand's `addColumn()`, records from `newRecord()` — all naming concrete classes. Construction is the factories' concern; the interface describes what a value can be *asked*. Removal only relaxes the contract, so it is non-breaking. `ColumnInterface`/`RecordInterface` followed in RSRMID-2923.
  - **Empirical correction, and it must not be re-reversed: a constructor declared on an *interface* is fully enforced.** PHP exempts constructors only in **class-to-class** inheritance; an incompatible implementation of an interface constructor is a declaration-time fatal. An earlier revision of this entry asserted the opposite from memory and made it the justification for a directive. The drift persisted for a mundane reason instead: `$context` is **optional**, and adding an optional parameter is legal widening. The decision is unaffected — but "an interface constructor is unenforced" is false. One 10-line script settled it; a language rule asserted from memory is worth 10 lines.
  - Related, and now historical: `CNR\Response::addColumn()` carried a `@psalm-suppress MoreSpecificImplementedParamType` for narrowing `array<array-key,mixed>` to `string[]` against the interface declaration. RSRMID-2939 removed that declaration, so nothing narrows and the suppression had to go with it — a never-applied suppression fails the lint (`UnusedIssueHandlerSuppression`). Net **loss** of one suppression. The general point stands: a suppression for an ordinary method's parameter variance is not the same defect class as an interface/implementation mismatch.
  - **To find this defect class again:** compare `getNumberOfParameters()` of every interface method against each implementation. **Guard:** `tests/ResponseInterfaceConsumerTest.php`.
- **Transport tuning is the caller's decision, not a brand default (RSRMID-2915, breaking in v20.0.0).** `IBS\Client`'s `getDefaultCurlOpts()` override forcing `CURLOPT_IPRESOLVE => CURL_IPRESOLVE_V4` is removed — it was a workaround for a handful of customers' IPv6-hostile hosts, hard-coded into everyone's transport. Affected integrations opt in with `setExtraCurlOptions()`, the API added in RSRMID-2913 deliberately *before* the default was withdrawn.
  - **The `getDefaultCurlOpts()` hook itself stays**, even with zero overrides: `resetCurlOptions()` is defined in terms of it (restoring defaults rather than blindly wiping the bag). **Revisit** only for a genuinely protocol-mandatory option — never to paper over one environment's networking.
  - **Consequence for callers:** an opted-in option is *caller* state, so `resetCurlOptions()` discards it, whereas IPv4 used to survive a reset as a brand default.
  - **Guard:** `ClientConfigSeamTest::testNoBrandOverridesTheDefaultCurlOptsHook()` and `testClientsHaveNoCurlOptionDefaultHook()` (the hook moved to `AbstractSocketConfig` with the bag in RSRMID-2921).
- **The caller wins on cURL options, except on a closed set the transport owns — and rejection is loud (RSRMID-2919, breaking in v21.0.0).** `HttpTransport::post()` did `curl_setopt_array($h, [defaults] + $options)`, and PHP's `+` keeps the **left** operand, so 11 keys never reached the wire: a caller asking for a 5s timeout silently got 300s while the client's bag and the unit tests both looked right. The operands are swapped, and transport-owned keys are rejected rather than ignored.
  - **Rejected: simply swapping the operands.** The 11 colliding keys were three different things and only one was a defect. *Envelope invariants* (`URL`, `POST`, `POSTFIELDS`, `RETURNTRANSFER`, `HEADER`) must stay unoverridable or parsing breaks — `RETURNTRANSFER => 0` makes `curl_exec()` return `true` and the parser gets nothing. *Security posture* (`SSL_VERIFYPEER`/`SSL_VERIFYHOST`) was protected only by accident, but a generic convenience bag is the wrong place to disable certificate verification. *Legitimate tuning* (`TIMEOUT`, `CONNECTTIMEOUT`, `HTTPHEADER`, `USERAGENT`) is where the bug was, and is now caller-overridable.
  - **`HttpTransport::PROTECTED_OPTIONS` is public** so consumers and tests can read it, and passing any of them raises `UnsupportedFeatureException` naming the constants **before the handle is touched** — a rejected option is a programming error, not a network event, and must not cost a request. Silence was the whole defect; do **not** downgrade this to an ignore.
  - **`CURLOPT_HTTPHEADER` is additive, and the transport's own four lines (`Expect:`, `Content-Type`, `Content-Length`, `Connection`) are protected too**, matched case-insensitively. `Content-Type`/`Content-Length` derive from the POST body and `Connection` from the reused handle, so overriding one corrupts the request exactly as overriding `CURLOPT_POSTFIELDS` would — and a wrong `Content-Length` fails *silently*. Replacing wholesale would drop the transport's entries; appending blindly would put two `Content-Type` lines on the wire and leave the winner to the server; so a collision is an error, not a merge. A non-array `CURLOPT_HTTPHEADER` is deliberately left for cURL's own `TypeError` (verified: `curl_setopt_array()` raises before `curl_exec()`, so nothing is sent).
  - **A negative socket timeout is rejected at the setter** via `InvalidConfigurationException`. Not gold-plating: verified on PHP 8.3.31 that `curl_setopt($h, CURLOPT_TIMEOUT, -5)` returns `false`, and `HttpTransport` does not inspect `curl_setopt_array()`'s return — so a negative value would be dropped with no signal, the same silent divergence reintroduced by the very setter added to end it. Use this type for *configuration values out of range*, as distinct from `UnsupportedFeatureException` (capability absent).
  - **The lesson the old test taught: "in the bag" is not "on the wire".** `testSetExtraCurlOptionsMergesOverTheBag()` asserted the bag and passed for two releases while half the options it described were discarded. Assertions about transport behaviour must observe the *effective* arguments. **Guards:** `tests/HttpTransportCurlOptionsTest.php` (rejection, offline; `testProtectedOptionsAreExactlyTheEnvelopeAndTlsKeys()` pins the list in both directions), `tests/Functional/HttpTransportTest.php` (the merge over a loopback socket, asserting the effect not the option value), `CNICTEST\Support\SpyTransport` (what the client hands the transport).
- **Client contract segregation, resolved per-method rather than wholesale (RSRMID-2911).** `AbstractClient` was a CNR base wearing a hat: `IBS\Client` overrode four methods only to throw and no-opped a fifth.
  - `useHighPerformanceConnectionSetup()` is **brand-agnostic** and stays on `AbstractClient`, opt-in on IBS/Moniker too — the caller supplies the local proxy.
  - `getSession()`/`setSession()` were kept on the base here on "harmless null object" grounds. **That sub-decision was reversed in RSRMID-2920** — see the session-seam entry.
  - `setRoleCredentials()` is **genuinely CNR-only**: it depends on the role separator, `""` everywhere but CNR, so inheriting it on IBS would silently forge a garbage `<uid><role>` login. It moved to `RoleCredentialsInterface` (implemented only by `CNR\Client`); shared-type consumers narrow via `instanceof`. Do **not** re-add it to `AbstractClient`.
  - `autoIDNConvert()`'s IBS override was dead code behind a flag. **This is the one method the per-method resolution got wrong** — it kept CNR rules on the shared base and settled for a flag disabling them, the same shape as the session null objects. Finished in RSRMID-2922; see the IDN entry.
- **The session seam is CNR-only, and absence is the chosen policy (RSRMID-2920, breaking, v22.0.0).** `AbstractSocketConfig` carried five CNR concepts as **null objects** while `CNR\SocketConfig` re-declared and overrode all of them, so session state existed in two places — and because the setters are fluent, `ClientFactory::ibs()->setSession("SESSION-ABC")` looked accepted and was discarded. The five accessors, `$session`, `$persistent` and `$roleSeparator` now exist **only** on `CNR\SocketConfig`; `persistent=1` is emitted by CNR's own `getPOSTDataParams()` (appended last, so the encoded body is byte-identical) and `AbstractSocketConfig::getPOSTData()` is purely the `http_build_query()` step. `getSession()`/`setSession()` moved to `CNR\Client`, and the empty `IBS\SessionClient`/`MONIKER\SessionClient` were deleted.
  - **The policy this settled, and the most citable rule in this file.** The project held two opposite answers for one defect class — a capability the platform cannot honour. RSRMID-2911 chose *silence* (null objects, recorded as "harmless"); RSRMID-2919 chose *noise* (`UnsupportedFeatureException`). An integrator learned which applied by testing. The third answer, available only here, is **absence**: nothing to call means a static-analysis error at the call site, which beats both a runtime throw and a silent discard. **Prefer absence where the type system can express it; where a method must exist on the shared surface, throw. Never no-op.**
  - **`MONIKER\Client` stays non-final on purpose.** Psalm's `ClassMustBeFinal` fired once nothing extended it, but that rule only ever fires on leaf classes — `CNR\Client`/`IBS\Client` escape it purely because something inside the repo extends them, so sealing one brand on that accident would make consumer extensibility differ per brand for an inexplicable reason. Suppressed at the class with that rationale.
  - **`CNR\Client::getSocketConfig()` is the single narrowing point** from `AbstractSocketConfig` to `CNR\SocketConfig`, because PHP typed properties are invariant — a subclass cannot re-declare `$socketConfig` narrower, so the covariant `newSocketConfig()` factory cannot inform the property's type. It uses an `instanceof`-guard-with-throw, never `assert()`. There is deliberately **no alias**: two methods narrowing the same property would be two places to keep in step. RSRMID-2966's per-brand constructor parameter is not a second narrowing point of that property — it narrows an *input*, and it is what keeps the `instanceof` guard unreachable for correctly-typed callers now that the property has two writers.
  - **Guard:** `tests/ClientSessionSeamTest.php`. Absence cannot be tested behaviourally — calling a method that is gone is a fatal `Error` — which is exactly why the stubs survived from v11 to v21.
  - **The CNR session lifecycle lives on `CNR\Client` itself; `SessionCapable` and `SessionClient` are retired (RSRMID-2969, breaking, v33.0.0).** `login()`/`logout()`/`saveSession()`/`reuseSession()` were a 97-line trait with exactly one possible host, consumed by a 21-line subclass whose entire body was `use SessionCapable;`. `ClientFactory::cnr()` never once handed out the session-less parent, so the split bought a distinction no code in `src/`, `tests/` or `examples/` made — while costing three file opens to find `login()`. The trait's own `@psalm-require-extends Client` was it conceding that it was only ever part of this class. `cnr()` now returns `CNR\Client`.
    - **Rejected: keeping `SessionClient` as a `class_alias`.** It looks like free backward compatibility and is not. PSR-4 needs a file at `src/CNR/SessionClient.php` for the name to resolve at all, so the file count barely moves; and an alias *is* its target, so `assertInstanceOf(SessionClient::class, CF::cnr())` would keep passing while proving nothing — the exact vacuity the ticket warned about. If a major is being paid anyway, pay it cleanly. That assertion is now `assertSame(Client::class, CF::cnr()::class)`, matching how IBS and MONIKER were already asserted.
    - **Rejected: an empty `class SessionClient extends Client {}`.** That is the silhouette RSRMID-2920 deleted from IBS and MONIKER. It would not be the same defect here — CNR's capability is real — but reinstating the shape on the one brand that kept it invites the reader to conclude the deletion was arbitrary.
    - **Not a widening of the interface-coverage sweep.** Folding four public methods onto `CNR\Client` does not drag them into `tests/InterfaceCoverageSeamTest.php`, because that sweep subjects only classes implementing a *total* interface and `CNR\Client` implements just `RoleCredentialsInterface`, which is deliberately excluded from that role. Checked rather than assumed.
    - **What the fold bought in coverage.** Two `login()` branches had sat marked `//TODO -> not covered` — a transport failure, and a success carrying no `SESSIONID` — reachable offline the whole time via `setTransport()`, simply unwritten because the lifecycle was only exercisable through `SessionClient` with cassettes. Both are now driven through `SpyTransport`, which gained an optional canned-error constructor argument for the purpose. The transport-failure test pins something sharper than it looks: a cURL failure is CNR code **421**, so `isTmpError()` is true while `isError()` is **false**. `login()` therefore has to guard its session write on `isSuccess()`; rewriting that as `!isError()` would overwrite a live session with an empty string on every timeout, and now fails a test.
    - **Guard:** `ClientSessionSeamTest::testTheCnrSessionLifecycleIsNotBehindATraitOrSubclass()`. Structural because re-extracting is behaviour-preserving on the day it lands — a trait composed back into `CNR\Client`, or a subclass reinstated in front of it, keeps every call site working and every other test green while restoring the indirection. **Revisit condition:** a genuinely session-less CNR client becoming a real use case, which is a new type with a narrower contract, not this trait extracted back out.
- **Connection configuration has exactly one home, and two invariants keep it that way (RSRMID-2921, breaking, v23.0.0).** State was split between `AbstractClient` and `AbstractSocketConfig` with no invariant tying the copies together. The root cause was structural: **there was no accessor for `$socketConfig` on the client**, so every value needed a hand-written forwarder or was unreachable — which is why 18 accumulated and why `AbstractClient` was the repo's highest-churn file (43 touches in 120 commits).
  - **The division of labour.** The config owns *the connection*: where to connect (`$url`, `$oteUrl`/`$liveUrl`, `$highPerformance`), how to authenticate, and how the transport behaves (`$socketTimeout`, `$proxy`, `$referer`, `$curlOptions`). The client owns *client behaviour*: logger and `$debugMode`, `$context`, the transport instance, and the SDK's identity (`VERSION`/`$userAgent` — kept on the client because semantic-release rewrites `VERSION` in that file).
  - **Invariant 1 — the system is derived, never stored.** `getSystem(): ?System` compares `$url` to the two endpoints. The nullable return is the point: after `setURL("https://staging.example/")` there is no honest OT&E-or-LIVE answer, and reporting the last selection is exactly how the `useOTESystem()->setURL($custom)` drift happened. Taken in the direction that keeps `setURL()` authoritative.
  - **Invariant 2 — a value the SDK models cannot also come from the cURL bag.** `AbstractSocketConfig::MANAGED_OPTIONS` (`CURLOPT_TIMEOUT`/`USERAGENT`/`PROXY`/`REFERER`) is rejected by `setExtraCurlOptions()` with an exception naming the owning **class and** setter — each already has one, and a bag value silently outranked it while the getter reported the setter's. Rejection is **eager** here (the config knows immediately) whereas `PROTECTED_OPTIONS` is checked on the next request (the transport is injectable, and which options it owns is its own business). Options the SDK has no opinion about — `CONNECTTIMEOUT`, `IPRESOLVE`, `HTTPHEADER` — stay caller-owned. **Guard:** `AbstractClientCurlOptionsTest::testManagedOptionsAreExactlyTheOnesWithTheirOwnSetter()`.
    - **Each entry carries the class that owns the setter, and the message renders it (RSRMID-2944, non-breaking, v32.0.0).** The four setters are not all on one object — `setUserAgent()` lives on `AbstractClient`, because the SDK's identity is versioned with that class — so the original bare `"setUserAgent()"` pointed a caller holding only the config at a method it cannot reach. **Do not "simplify" the `owner` key away, and do not drop `CURLOPT_USERAGENT` from the list instead**: dropping it re-opens the second home the constant exists to close. The guard had accepted the setter existing on *either* class (`method_exists(config) || method_exists(client)`), which is precisely what let the unreachable name pass review; it now checks against the class the entry names, plus a behavioural test that the thrown message carries it.
  - **High-performance routing is a flag applied inside `getURL()`, not a URL rewrite.** The eager rewrite was itself a drift: the loopback URL no longer matched the OT&E endpoint, so switching the mode on silently cost the caller `isOTE()`. Routing is *how* to reach the endpoint, not *which* one, so the selected system now survives it and it survives a later system switch.
  - **The 18 forwarders were deliberately kept**, against the ticket's "collapse them" option: they are the documented consumer surface, and deleting them would rewrite every integration to fix nothing — a forwarder is one home with one answer, and the invariant is "no value readable from two places **with two possible answers**". What `getSocketConfig()` changes is that a *new* setting no longer needs one — so do not add one; `getSocketConfig()->getOTEUrl()` is the answer. **Rejected** in the opposite direction: absorbing the config into the client would cost the "configure without constructing a client" property that motivated the ticket.
  - **`getCurlOptions()` puts dedicated proxy/referer state on the *left* of the union, and that is load-bearing.** `setExtraCurlOptions()`'s guard is not the only writer of `$curlOptions` — `getDefaultCurlOpts()` seeds it and `resetCurlOptions()` re-seeds it, neither passing the guard. A brand default of `CURLOPT_PROXY` — exactly the protocol-mandatory case that hook is kept for — would bring the drift back **inverted**: the getter reporting the setter's value while the request used the default. `executeCurl()`'s third `$extraCurlOpts` parameter was removed for the same reason: an option route skipping `MANAGED_OPTIONS` is a way to put a second answer behind `getProxy()`. **Guard:** `AbstractClientConfigDriftTest::testDedicatedProxyStateBeatsABrandDefaultOnTheWire()`, which writes out the brand subclass that does not yet exist.
  - **Deliberately not addressed:** with debug mode on, `performRequest()` calls `getPOSTData()` twice to build the masked log line. It is not a second *home* — both calls read the same config and cannot disagree — and a masked variant genuinely needs a second pass; the alternatives (a `getPOSTDataPair()`-shaped API, or masking the encoded string) are not worth a debug-only saving.
  - **Guards:** `tests/ClientConfigSeamTest.php`, `tests/AbstractClientConfigDriftTest.php`.
- **The seam got its build half: clients accept a pre-built `SocketConfig` (RSRMID-2966, non-breaking, shipped in the v33.0.0 batch).** RSRMID-2921 left the seam half-finished. `getSocketConfig()` covered reading and post-hoc writing, but there was no way to *supply* configuration — so `ClientConfigSeamTest::testAConfigIsUsableStandalone()` had been asserting since v23 that a config is buildable on its own, a capability with no consumer: the only config any client could ever use was the one its own `newSocketConfig()` minted. `__construct(?AbstractSocketConfig $socketConfig = null)` connects it. Purely additive — omit the argument and the null branch runs the previous statement verbatim — so no existing consumer changes, and the major in v33.0.0 comes from RSRMID-2965, not from here.
  - **The motivation is a window, not ergonomics.** A default-constructed client is already aimed at **LIVE**, so an incomplete setter sequence does not fail loudly — it yields a correctly configured client pointed at production. Config-first has no such window: the client does not exist until its configuration is complete. The secondary win is that configuration becomes a value a caller can produce in one place and consume in another (a WHMCS module reading stored settings once), which the client-only route made impossible because the return type had to be a client.
  - **The parameter is narrowed per brand, and that is the load-bearing part.** `CNR\Client` takes `?CNR\SocketConfig`, `IBS\Client` `?IBS\SocketConfig`, `MONIKER\Client` `?MONIKER\SocketConfig`. Typing it `?AbstractSocketConfig` once would compile, break nothing, and pass every behavioural test — while making `new MONIKER\Client(new IBS\SocketConfig())` legal, silently pointing a Moniker client at the IBS host, since endpoints are the *only* difference between those two brands. PHP exempts constructors from LSP under class inheritance, which is the sole reason a subclass can narrow a parameter here at all; both analysers accept the declaration and enforce it against call sites (verified by spike before implementing — PHPStan reports `argument.type`). Do **not** "fix" a brand by widening it to match the parent.
  - **A supplied config is adopted by reference, never cloned.** A defensive `clone` would be RSRMID-2921's two homes reintroduced as a copy the caller cannot see, and behaviour-preserving on the day it lands — the clone agrees with the original until one side is written. Hence an identity guard rather than a behavioural one.
  - **Rejected: taking `$context`, `$transport` or a logger too.** The AC's "fully configured in one call" reads wider than the change, and deliberately so — it covers *connection* configuration. Everything the config does not carry is a plain single-property assignment with no side effect and no ordering rule, so moving it into the constructor buys a second spelling rather than a closed hazard. Specifically: `Cassettes::attach()` builds the transport around a client that already exists, so a transport parameter would be unused by the repo's own transport-injection consumer; and `$context` has a per-request lifetime (it is copied into every Response and legitimately changes between requests on one client), so freezing it at construction would add a route without retiring `setContext()`. Note the ordering rule this therefore does **not** fix: `setLogSink()` before `setCustomLogger()` is logger state, still order-dependent.
  - **The forwarder count reconciled, and a one-writer gap closed.** `FORWARDED_METHODS` listed 16 entries against 17 same-name forwarders — `getPOSTData` was missing, invisible because nothing failed. `setCredentials` is the 18th and **cannot** be listed: it fans out to `setLogin()` + `setPassword()`, so the config has no method of that name and the counterpart assertion would fail on it. It gets its own two-writer check instead; 17 + 1 is the 18 that `AbstractClient`'s class docblock counts. Do not "complete" the list to 18 by adding `setCredentials`.
  - **`#[\Override]` already covers part of that check, which narrows what the guard is for.** Renaming `setLogin()`/`setPassword()` on `AbstractSocketConfig` while `CNR\SocketConfig` overrides them is a fatal error, not a guard failure — the overrides carry `#[\Override]`. The guard therefore exists for the mutation the language does not catch: dropping the pair from both levels in favour of one combined setter. The behavioural half (credentials discard an active session, *including* when the config arrived pre-built — the ticket's own AC) is a separate test.
  - **Guard:** `tests/ClientConfigSeamTest.php`, extended not replaced. Proven non-vacuous by eight mutations, each observed red: defensive `clone`; MONIKER's parameter widened to the IBS config; the parameter made required; the parameter renamed on one brand; MONIKER's default config replaced by the IBS one (which is why that assertion compares the exact class — `MONIKER\SocketConfig extends IBS\SocketConfig`, so `instanceof` would pass and hide the mix-up); `getPOSTData` dropped from the client; both credential writers dropped from the config; and the clear-session rule pre-empted at construction.
  - **The test win is measured, not asserted.** `tests/CNR/ClientTest.php` held a shared static client with 7 hand-undo sites and a hidden ordering chain — `testSetSessionReset()` asserted a login that `testSetSessionCredentials()` had left behind, and `testSetSessionSet()` depended on a `setCredentials()` reset several tests earlier. Sixteen tests now own their client, stating preconditions through a config where a starting state matters. Before the change `testSetSessionReset()` and `testSaveReuseSession()` **failed when run standalone**; all sixteen now pass in isolation. `$userAgent` tests are deliberately left sharing: the user agent is client state, outside this seam.
- **CNR IDN command rewriting is a module, not a base-class method behind a flag (RSRMID-2922, breaking, v24.0.0).** `AbstractClient::autoIDNConvert()` was the largest method in that class and every line was CNR domain knowledge — which parameters carry a domain name (`PARENTDOMAIN|NAMESERVER|NS|DNSZONE` with an optional index; `DOMAIN` deliberately absent, the API converts that itself), and that `OBJECTID` is a *pattern* parameter whose content depends on `OBJECTCLASS` (RSRTPM-3167). It ran on a base shared with IBS/Moniker behind `needsIDNConvert`, a flag whose only job was to disable it for two brands out of three.
  - **The fix used a hook that already existed.** The rules live in `CNIC\CNR\IDNCommandRewriter::rewrite(array $cmd): array` — a `final` class of static rules, same shape as `CommandFormatter` — called from `CNR\Client::buildCommand()`. No brand detection replaced the flag, because the call site *is* the brand. **The generalisable form: when brand behaviour needs a flag on a shared class, the hook it should have used is usually already there.**
  - **Order matters and is pinned.** The rewrite runs *after* `flattenCommand()` (the rules match wire keys like `NAMESERVER0`, not the caller's nested input) and rewrites values in place, so the priority sort survives it.
  - **What stayed:** public `AbstractClient::IDNConvert()` — converting a caller-supplied list is brand-agnostic and has been public API since v7.1.0. **What went, breaking:** protected `autoIDNConvert()`, `needsIDNConvert` on all three configs, and `AbstractSocketConfig::getNeedsIDNConvert()`.
  - **The testability argument is why this was a ticket at all.** With no surface of its own, the behaviour was only reachable through `ReflectionMethod::setAccessible()`, which two test files did. **Where a test has to bypass the interface to reach the behaviour, the module is the wrong shape.** Note the distinction the repo now keeps by filename: reflection to *test behaviour* is the smell this removed; reflection to *guard absence* is unavoidable and lives in `*SeamTest.php`.
  - **Guards:** `tests/ClientIDNSeamTest.php` (absence of `autoIDNConvert()`/`needsIDNConvert`), `tests/AbstractClientIDNTest.php`, `tests/CNR/IDNCommandRewriterTest.php` (10 cases, including both halves of the `OBJECTID`/`OBJECTCLASS` decision and the absent-`OBJECTCLASS` case the reflection tests never covered).
- **Config-driven, and brand state stays on the brand config.** Each `SocketConfig` carries the API config as typed properties rather than the per-namespace `config.json` files this replaced.
- **Type-hint against the interface, not the concrete class** (`LoggerInterface` not `Logger`). Narrow to `ExtendedResponseInterface` only for the 5 CNR-only Response capabilities, and to `RoleCredentialsInterface` only for CNR role credentials.
- **Edit the shared pipeline in the abstract, the brand differences in the hooks** — the brand `ResponseTemplateManager`/`ResponseTranslator` classes hold only per-brand data plus hooks. Do not reimplement a pipeline per brand.
  - **A brand that rewrites no descriptions declares nothing (RSRMID-2970, non-breaking, no bump).** `descriptionRegexMap()`/`descriptionRawPatternMap()` on `AbstractResponseTranslator` default to `[]` instead of being abstract. IBS rewrites nothing, and used to say so in four members — two empty `private const` maps plus two hooks returning them, ~28 lines of boilerplate asserting an absence. It now declares neither and inherits the defaults. CNR still overrides both, because it has real content. **This is not the "never no-op" case**: that directive is about a capability the platform cannot honour, where silence hides a caller's mistake, whereas an empty rewrite map is a complete and truthful answer to "which messages does this brand rewrite?" from a brand whose answer is "none". Note the asymmetry that made this safe to change: `newTemplateManager()` right beside them carries an explicit docblock defending *why* it is abstract; these two carried none, so their abstractness was incidental rather than decided.
  - **Rejected, and do not re-raise without new evidence: replacing the `translate()` and `newResponseParser()` hooks with class-name constants.** The observation behind it is real and still true — both brand `translate()` bodies are byte-identical apart from an import alias (and now *five* parameters wide, since RSRMID-2968 added `$templates`), and `return new RP();` appears in four concrete places. But the mechanism is worse than the duplication. Dispatching through a constant means variable static calls (`$cls::translate(...)`) and variable instantiation (`new (static::PARSER)()`), which PHPStan L9 and Psalm L1 type poorly — and the ticket's own acceptance criterion forbade a new suppression. It also trades overridable `protected` hooks for constants, removing an override seam **immediately** after RSRMID-2968 leaned on `newResponseParser()` being a per-brand hook (`AbstractResponseTemplateManager::isTemplateMatchPlain()` calls it) and documented it as the template-manager twin. Reopening this requires re-running both analysers on a prototype, not citing the ~30-line count.
- **`ClientFactory` exposes typed named constructors** — `cnr(): CNR\Client`, `ibs(): IBS\Client`, `moniker(): MONIKER\Client` — not a string-dispatch `getClient(string)`. Each returns the concrete brand client fully typed, so brand capabilities need **no** narrowing on the normal path. The old `Registrar` enum and `UnknownRegistrarException` went with `getClient()`: an unknown registrar is now impossible by construction. Where code *deliberately* holds the shared `AbstractClient` and reaches a brand method, use an `instanceof`-guard-with-throw, **never `assert()`** — PHP compiles `assert()` out when `zend.assertions` is disabled, so it cannot carry a runtime guarantee. (The `assert($r instanceof Response)` in each brand's `request()` is a different thing: a static-analysis hint about a return type the factory hook already guarantees.) (RSRMID-2911.)
- **The request lifecycle is a template method, and `request()` is symmetric across brands (RSRMID-2909, superseding RSRMID-2864).** The shared pipeline (build command → resolve URL → serialise POST → cURL → construct Response → optional debug log) lives once on `AbstractClient::performRequest()`; brands vary it through exactly **two hooks plus their config subtype** — `buildCommand()` (CNR flattens as-is, IBS injects `ResponseFormat=JSON`), `newResponse()`, and `newSocketConfig()`, where a brand-mandatory cURL option would now go. **No brand actually varies the transport bag** — every brand starts from an empty one. Every SocketConfig configures the **host only**: CNR appends its default `api/call.cgi`, IBS/Moniker pass a per-operation `$path`. The symmetry is deliberate: RSRMID-2909 **dropped** the earlier "CNR must never accept a per-request path" rule rather than preserving it. Never reimplement `performRequest()`.
- **Date/time values are parsed on demand, never rewritten into the response (RSRMID-2318; RSRMID-2926).** `CNIC\ApiDateTime` is a standalone, immutable, UTC-only value object — deliberately **not** a `\DateTimeInterface` — turning the two shapes the APIs emit (`Y-m-d H:i:s` with optional fraction from CNR; `Y/m/d` from IBS/Moniker) into one flat struct: `?int $ts`, `string $date`, `?string $dateTime`, `string $tz`, `string $raw`, with `tz` structurally pinned to `"UTC"` by a private constructor. It is never invoked during response construction; `src/` reaches it from exactly two opt-in accessors, and `getPlain()`/`getHash()`/`getListHash()` still return raw API strings.
  - **Opt-in parser, not a response transformation.** **Rejected:** the original proposal to rewrite date columns into a nested `{original, localized}` structure — breaking for every consumer, and it bakes a server-side timezone assumption into data.
  - **Parser, not formatter.** No `in($tz)`, no locale formatting, no `ext-intl` (which the WHMCS `castDate()` helper needed and the SDK will not take on), no view-layer markup — `toHtml()`/`assetPath()` were dropped when localization was split to RSRMID-2916. Consumers localize from `json_encode($dt->toArray())` or `new \DateTimeImmutable("@{$dt->ts}")`. The field names are a superset of `castDate()` so that integration can drop its private copy.
  - **Strict two-stage parse, because neither stage alone suffices** (verified on PHP 8.3.31). An anchored regex gate runs first, because `createFromFormat("!Y-m-d", "2026-7-1")` succeeds with *zero* warnings; then `createFromFormat()` in an explicit UTC `\DateTimeZone` (so the process default cannot shift the instant), rejecting on **any** `getLastErrors()` warning — PHP reports out-of-range components as a *warning* while still returning a rolled-over object (`2026-13-45` → `2027-02-14`, `2026-02-30` → `2026-03-02`, `0000-00-00` → `-0001-11-30`, `25:00:00` → next day). A `$parsed === false` check alone lets all of those through. Fractional seconds are matched and discarded; offset-bearing values (`Z`, `+02:00`) and the ISO `T` separator are rejected — the accept-set is exactly what the APIs emit.
  - **`ts` and `dateTime` are null together for date-only values; `date` is always populated.** `dateTime` deliberately does **not** fall back to `date`: `strtotime($dt->dateTime)` on a fallback returns midnight UTC, precisely the fictitious instant `ts === null` exists to refuse, whereas null fails loudly (`strtotime(null)` is a TypeError on PHP 8.1+). `tz` stays `"UTC"` even then: it describes the source *declaration*, not an instant. `castDate()`'s `tzAbbr` is **not** carried over — it held the abbreviation *at that instant* (`CET`/`CEST`), a distinction that cannot exist in a UTC-only type with no DST, so it would duplicate `tz` forever.
  - **Separator tolerance, both sides consistent (RSRMID-2926).** `PATTERN` accepts `-` or `/` via a captured `sep` group plus a `\k<sep>` backreference, so `2026-02/20` and `2026/02-20` are still rejected — as strict about shape as before, over a wider accept-set. `$date`/`$dateTime` always emit `-`, normalised before `createFromFormat()`, so the struct's shape never depends on which brand sent it. Still a parser: no new public method.
  - **`$raw` exists because the accessors closed the gap that used to guarantee the caller still had it.** Before `getDateTimeByKey()`/`getDateTimeByIndex()`, the only route was `ApiDateTime::tryFrom($hash["paiduntil"])`, so a caller necessarily still held the original string. It is populated from the constructor's `$value` at both `from()` call sites — **never** from the separator-normalised subject — and is the only place the fractional precision `$dateTime` discards survives. **Display/logging/round-trip only:** comparing or sorting on `$raw` reintroduces exactly the separator bug `$date`'s normalisation prevents (`"2026/02/20"` sorts wrong against `"2026-03-01"`). No getter, no `withRaw()`.
  - **`Record`/`Column` gained one opt-in accessor each, not a shared helper.** Each is a one-line narrowing (`is_string($v) ? ApiDateTime::tryFrom($v) : null`) declared on both the class and its interface. The line is duplicated deliberately: `Record` and `Column` have no composition relationship, so sharing it would either invent one or add an indirection with nothing behind it. Both interface declarations widen the public contract, which is what makes this breaking (v28.0.0) independent of the separator change. **RSRMID-2942 (v32.0.0) generalised the shape** — the same one-line, interface-declared narrowing now covers strings too, and it is what replaced the erased `CNR\Column` generic.
  - **The IBS date-separator rewrite is deleted, and deliberately not guarded.** `IBS\ResponseParser` used to `array_walk_recursive()` and rewrite `/` to `-` in values whose key matched `/(date|paiduntil|expiration)$/i` — the only place in `src/` that mutated raw response data, and over-broad enough to turn `n/a` under an `updatedate` key into `n-a`. No guard is needed because re-adding it is **not** behaviour-preserving: it changes `getHash()`/`getPlain()`/`getListHash()` for every IBS/Moniker date field, so the ordinary parser/response/record/column assertions already catch it.
  - **The interface declarations are protected by `#[\Override]`, not by a test** — dropping them from `RecordInterface`/`ColumnInterface` while leaving the methods on the classes would otherwise be behaviour-preserving for the whole suite. `#[\Override]` makes it a declaration-time fatal, which is why no `*SeamTest.php` was added. **The corollary: stripping `#[\Override]` from either implementation silently removes the only thing protecting the interface declaration.**
- **Failures throw from one hierarchy, and growing it is not a breaking change (RSRMID-2895).** Every exception extends `CNIC\Exception\CnicException extends \Exception`. Reuse the existing types rather than duplicating them: `UnsupportedFeatureException` (a capability this platform or response does not have — since RSRMID-2919 also a cURL option or HTTP header the transport owns), `PaginationException`, `InvalidConfigurationException` (a configuration value out of range), `InvalidDateTimeException`.
  - **Additive by construction, which is the point.** Because everything ultimately extends `\Exception`, existing `catch (\Exception)` consumers keep working when a subclass appears — so a new failure mode needs **no** `BREAKING CHANGE:` footer and no major bump. That is what makes "add a subclass" the cheap option and a bare `\Exception` the expensive one: a bare throw is indistinguishable from a runtime bug at the call site and cannot be caught selectively ever after.
  - **Do not create ad-hoc exception types outside `CNIC\Exception`.** The failure modes are the SDK's public vocabulary; a type declared next to the code that throws it is invisible to a consumer writing catch blocks against the namespace.
  - Related: absence first, throw second, never a no-op — see the session-seam entry.
- **A public parameter name is public API, so renaming one needs a major (RSRMID-2935, breaking, v29.0.0).** Since PHP 8.0 a caller may bind an argument by name and the runtime floor here is 8.3, so consumers can already do it. PHP has no parameter alias, so there is **no deprecation path**: after a rename the caller gets `Error: Unknown named parameter $key` at runtime, with a message that does not point at an SDK upgrade. Every public parameter whose name carried no meaning was renamed in one batch for that reason; table in [MIGRATION.md](../../MIGRATION.md).
  - **A named argument binds to the *implementation's* parameter name, never the interface's** — verified on PHP 8.3.31, and the opposite of what an earlier revision of this entry claimed. Given `interface I { function get(string $columnName); }` and `class C implements I { function get(string $key); }`, a call through an `I`-typed variable accepts `get(key: …)` and rejects `get(columnName: …)`. **The consequence that matters: the implementation's names are the real contract, and typing against an interface does not insulate a consumer from an implementation rename.** Renaming only on the interface breaks no caller (only Psalm objects); renaming only on an implementation breaks callers while the interface looks untouched.
  - **The rename is what let the annotations go, and that was the point.** Meaningless names (`$value`, `$key`, `$id`, `$idx`, `$h`, `$r`, `$ph`, `$rr`, `$str`, `$rv`, `$uid`, `$pw`, `$secured`, `$toupper`) each needed a `@param` line restating them; 48 are gone. What deliberately **stayed** is every annotation encoding something a name cannot: an array shape PHPStan L9 requires (`array<string,mixed> $row`), a precondition on the caller (`$post` is "already masked"), the semantics of an empty string (`$proxy`/`$login`/`$session` reset), and one cross-brand fact (IBS's `generateTemplate()` `$code` goes on the wire as `status`). Do not delete those to make the count rounder. Follow-up to RSRMID-2934, which did the same for internal parameters and needed no major.
  - **A `protected` property name is extension API on the same terms**, and six were renamed alongside: `AbstractSocketConfig::$pw` → `$password`, `AbstractClient::$ua` → `$userAgent`, `$curlopts` → `$curlOptions`, and `AbstractResponse::$paginationkeys`/`$columnkeys`/`$columnindex` → camelCase (the repo's only violations of its own naming rule). The break is a *different* one and worth keeping distinct: a named argument is a caller concern, whereas a protected property is reachable only from a subclass — a narrower audience but a **louder** failure, an undefined-property access PHPStan reports at L9. Public properties were left alone: `$templates` and `ApiDateTime`'s readonly fields are accurately named, and `System::$name`/`$value` are native enum properties that cannot be renamed.
  - **A property reachable only by reflection needs the guard's string literal updated too, and that is a real trap.** `ClientConfigSeamTest` and `AbstractClientConfigDriftTest` reach these through `new ReflectionProperty(…, "curlopts")`. Neither analyser resolves those literals, so a missed one is invisible to `composer lint` and surfaces only as a `ReflectionException` under `composer test`. Note what was done to `ClientConfigSeamTest::CONFIG_OWNED_PROPERTIES`: the old spelling was **kept** alongside the new, matching the file's `socketURL`/`url` precedent, so re-adding *either* still fails. Renaming rather than adding would have quietly narrowed the guard.
  - **The guard is Psalm's `ParamNameMismatch`, not PHPStan — and the ticket said otherwise.** RSRMID-2935 recorded that PHPStan L9 reports `argument.parameterRenamedInSubtype`; only the conclusion was right. Renaming the interfaces and `src/` implementations while leaving four test transports and five test loggers on the old names left **PHPStan completely clean**; Psalm caught all 14. Both analysers cover `src/` **and** `tests/`, which is what made the test doubles visible at all — a reason not to narrow that scope.
  - **No `*SeamTest.php` was added, deliberately — but know where enforcement stops.** A reflection test comparing parameter names would duplicate `ParamNameMismatch`, which runs over every implementation the analysers *read* rather than a hand-maintained class list; `InterfaceCoverageSeamTest` compares `getNumberOfParameters()`, not names, and should not be grown into a name check. The blind spot is **scope**: the analysers read `src/` and `tests/` only, so the two places a rename actually got missed were `README.md`'s custom-logger snippet and `examples/CustomLoggerClass.php` — both consumer-facing templates, and `README.md` is the only doc that ships in the Composer dist. **Grep those two by hand when renaming a parameter.** What no tooling catches at all is the breaking act itself: renaming interface and implementations in lockstep leaves everything green. That one is policy, not a test — it is why this entry exists.
- **Sensitive-command masking is one algorithm and one field list per brand, not two of each (RSRMID-2938, non-breaking).** Which command keys are secret was declared four times — byte-identical within each brand across its `SocketConfig` and `Response` — and the masking algorithm twice, differing only by a null check; both docblocks said "Mirrors …", documenting the drift risk rather than removing it. `CNIC\CommandRedactor::redact(array $command, array $sensitiveFields): array`, a generic static method (`@template TValue of string|null`) modelled on the existing `CommandFormatter`, now holds the one matching algorithm — case-insensitive key comparison, values masked to `CommandRedactor::MASK`, `null` values left untouched — and both `AbstractSocketConfig::maskSensitiveCommand()` and `AbstractResponse::sanitizeCommand()` delegate to it. Each brand's field list is declared exactly once, in a new `CNR\SensitiveFields`/`IBS\SensitiveFields` holder class (`public const array KEYS`), and both the brand `SocketConfig` and `Response` initialise their `$sensitiveFields` property from that one constant instead of repeating the literal array.
  - **Rejected: sanitizing once on the `AbstractClient`/`SocketConfig` side and deleting `AbstractResponse::sanitizeCommand()`.** Both `CNR\Response` and `IBS\Response` are publicly constructible directly with a raw `$cmd` (`tests/CNR/ResponseTest.php` does exactly this to assert masking). If the Response stopped self-redacting, `new CNR\Response($raw, ["PASSWORD" => $real])` would store the real password — the client-side path never runs. Each hierarchy must stay independently safe; centralizing removal of either side's redaction is a leak, not a simplification, so **both existing call sites are kept, and only the algorithm and the field list are unified.**
  - **The base `protected array $sensitiveFields = []` properties on both abstracts are kept, not removed.** They are non-`final`, existing override hooks; deleting them would silently stop redacting for any downstream subclass already relying on one, turning a "non-breaking" ticket into a silent security regression.
  - **The `SensitiveFields` holder lives next to both consumers, not folded onto the brand `SocketConfig` for the `Response` to import.** That would save two small files but make a `Response` class reach into connection-config territory, against the grain of the RSRMID-2921 rule that connection state has exactly one home; a plain shared constant costs less than the precedent of a Response importing a SocketConfig-namespaced class.
  - **The unified algorithm keeps the config side's null-skip**, which is load-bearing there: `http_build_query()`/CNR's command builder both drop `null` values, so masking one would log a key the real request never sent. Carrying the skip into `sanitizeCommand()` changes one case unreachable through the public constructor's `array<string, string>` parameter type (`["PASSWORD" => null]`) — deliberate, not an oversight, and no secret is exposed either way.
  - **All three new classes are `@internal`, and that is a deliberate scope call, not an oversight of the `@psalm-api` rule.** CLAUDE.md says public API symbols get `@psalm-api`; these get the opposite tag because none of them helps a consumer talk to the API. `CommandRedactor` exists only to keep this SDK's own two masking call sites in step, and the `SensitiveFields` holders only to stop two literals drifting. The extension point consumers actually use is unchanged and still public: override `$sensitiveFields` on a subclass. Marking them `@internal` keeps them out of the published Doctum surface and out of the set of signatures a major bump has to protect — **so treat a change to `CommandRedactor::redact()`'s signature as non-breaking, which is the whole point of the tag.** They need no `@psalm-api` either way: both are referenced from `src/`, so `findUnusedCode` never reports them.
    - **The lint consequence, which is the part that will bite:** Psalm scopes `@internal` to the *declaring root namespace*, so every reference from `CNICTEST\` raised `InternalClass`/`InternalMethod` — 20 errors, because both `src/` and `tests/` are in `projectFiles`. Fixed by suppressing exactly those two issues for `../../tests` in `psalm.xml`, matching the `UnusedClass` entry already there; `src/` stays strict. **Rejected: a `@psalm-internal CNICTEST` line in the `src/` docblocks** — it would work (multiple `@psalm-internal` annotations are OR'd) but puts the test namespace's name in shipped production docblocks, and "the package's own tests may reach package internals" is a testing policy that belongs in the linter config, not in `src/`. PHPStan does not enforce `@internal` across namespaces at all and stayed clean throughout — do not read its silence as the annotation being inert. Note both suppressions are load-bearing under `UnusedIssueHandlerSuppression`: delete the last test that touches these classes and the *lint* breaks, not just coverage.
  - **The masked command must not be re-sent as a request, and the unmasked copy lives *beside* the `Response`, never *on* it (RSRMID-2975, breaking only in what it now refuses).** `CNR\Client::requestNextResponsePage()` built the page-2+ request from `$currentPage->getCommand()` — the masked copy — so a list command carrying `AUTH` or `PASSWORD` re-sent the literal `CommandRedactor::MASK` as that parameter's value, **on the wire**, not merely in a log. Reproduced through `SpyTransport`: page 1 sends `AUTH=s3cr3t-auth-code`, page 2 sent `AUTH=***`. Fixed by having `CNR\Client` keep a `private \WeakMap<Response, array<string, string>>` of the exact command it handed each `newResponse()` call, and sourcing the continuation from it. The split is now explicit and worth stating as the rule: **command data comes from the command, pagination state (`LIMIT`, `LAST`) from the response** — the latter is never masked, the former always is.
    - **Rejected: an `$unmaskedCommand` property with a `getUnmaskedCommand()` accessor on the `Response`** — the reporter's own suggestion, and the intuitive one, since it needs no identity bookkeeping and works for a `Response` however it was built. It also undoes the guarantee this whole bullet exists to hold. Note carefully what the objection is *not*: the accessor grants no capability the caller lacks, because the caller already holds the command it passed to `request()`. It grants new **accidental**-leak surface — today `print_r($response)`, `var_dump()`, `json_encode()` and `serialize()` are all clean of command secrets because `$this->command` holds only masked values, and an unmasked property makes a full object dump in an error handler leak an EPP auth code. `__debugInfo()` mitigates exactly one of those four. Scoping the property to `CNR\Response` alone (the only brand that paginates) narrows the surface but does not change the argument. **Also rejected: a `getCommand(bool $masked = true)` parameter** — `getCommand()` is declared on `ResponseInterface`, so a new parameter is breaking, and v33 is simultaneously *removing* a `bool` parameter from `getColumnKeys()` for that same reason.
    - **The map miss throws only when something was actually masked, and that asymmetry is the point.** A `Response` this client did not produce (constructed directly, or from a second client instance) is absent from the map. Falling back to its own command is correct precisely when nothing in it was masked — the overwhelmingly common case, since no ordinary list command carries `AUTH` — so that keeps working untouched; when something *was* masked there is no unmasked copy anywhere to recover, so it raises `PaginationException` rather than putting the mask on the wire. This is the Node SDK's fix (RSRMID-2974) minus its stated weakness: there, a foreign `Response` silently degrades to the masked command, which is the defect left in place under a fix. Detection is by **value** (`in_array(CommandRedactor::MASK, …)`), not by key, so it holds for a subclass that widened `$sensitiveFields` past `SensitiveFields::KEYS`; the only false positive is a caller legitimately sending the literal `"***"`.
    - **Two corrections to the bug report, both of which make it quieter than filed.** (1) It cites RSRMID-2938 as introducing the masking — in the PHP SDK that ticket only *unified* it (`91f8124`); response-command masking predates it (`c44eb87`). (2) It states page 2+ "will fail authentication". It will not: the client's credentials travel as `s_login`/`s_pw` off the `SocketConfig` and were never in the command, so every page authenticates correctly. What broke is a **command parameter**, which the API may well accept — answering page 2 from a different result set than page 1 rather than erroring. The failure is silent, which is why it survived to be found by a parity review rather than by a user.
    - **No `*SeamTest.php`** — undoing this fix is not behaviour-preserving, so ordinary tests bite. `CNR\ClientTest::testRequestNextResponsePageSendsTheUnmaskedCommandParameters()` asserts on the bytes `SpyTransport` received, so it fails even when every in-process accessor looks right; the two companions pin the halves a one-sided fix would miss (a foreign unmasked `Response` still continues; a foreign masked one raises). All three were proven by mutation: re-pointing the continuation at `getCommand()`, refusing every foreign `Response`, and dropping the `newResponse()` recording each fail exactly one.
  - **No `*SeamTest.php` guard** — `tests/RedactionParityTest.php` pins that each brand's `SocketConfig` and `Response` defaults for `$sensitiveFields` are reflectively equal to each other and to `SensitiveFields::KEYS`, which is a value-equality check, not a structural seam, so it does not follow the guard-test docblock ritual. `tests/CommandRedactorTest.php` is `CommandRedactor`'s direct, brand-independent unit coverage — the redaction algorithm's first test surface of its own, previously reachable only through a brand config or response.
    - **MONIKER is asserted, not just asserted-in-prose.** `testMonikerInheritsTheIbsListRatherThanDeclaringItsOwn()` pins that `MONIKER\SocketConfig` still resolves to `IBS\SensitiveFields::KEYS` by inheritance — there is no `MONIKER\Response` and no MONIKER field list, and a MONIKER-local override that drifted from IBS would otherwise be the one drift this file's own anti-drift test could not see. Proven non-vacuous by adding `protected array $sensitiveFields = ["password"]` to `MONIKER\SocketConfig` and watching that test alone fail (the CNR and IBS cases stayed green, so the assertion is targeted rather than incidentally coupled).
- **The transport error is a declared parameter, not encoded into the payload (RSRMID-2937, breaking, v30.0.0).** `HttpTransport::post()` used to return the failure twice — as tuple element `[1]`, and as a `"httperror|"` prefix smuggled onto `[0]`, which `AbstractResponseTranslator::translate()` string-split back off with `explode("|", $newraw, 2)`. The sentinel is removed: `?string $error` is now an explicit trailing parameter, appended **last** and defaulted `null` on every hook in the pipeline (`TransportInterface::post()`'s tuple, `AbstractClient::newResponse()`/both brand overrides, `AbstractResponse::__construct()`/`translate()`/both brand overrides, `AbstractResponseTranslator::translate()`), so every existing positional call site keeps working. The two-level check is unchanged: `$error !== null` selects the `httperror` template, `$error !== ""` gates the `{HTTPERROR}` injection into it. `TransportInterface::post()`'s docblock now **states** the contract it always implied: a non-null `[1]` means `[0]` is unusable, and `HttpTransport` honours it by returning `["", $error]` on failure rather than any payload at all.
  - **`nocurl` is gone with the branch it existed for.** `curl_init() === false` is unreachable with `ext-curl` as a hard composer dependency; the guard is now `\assert($tmp !== false)`, matching the file's existing idiom, and the `"nocurl"` entry is deleted from both brand template sets (then `ResponseTemplateManager::$templates`, now `BUILTIN_TEMPLATES`). Do not re-add it without a real reachable failure mode to attach it to.
  - **`AbstractResponseTranslator::resolveTemplateId(string $raw, ?string $error, array $rawTemplates): ?string` is the extracted decision point, and it is where the raw-as-template-id mocking route becomes explicit rather than incidental.** A non-null `$error` resolves to `"httperror"` **only if `$rawTemplates` declares that id** — the registry is caller-supplied, so a third-party brand's need not include it, and indexing `$rawTemplates["httperror"]` unconditionally degrades an `Undefined array key` warning into a `TypeError` a few lines later. Otherwise `$raw` is checked against `$rawTemplates` and returned only if it matches — which is exactly what lets a test construct `new Response("empty")` or `new Response("myid", templates: $registry)` (after `addTemplate()` on that registry) and get a canned response with no real API round-trip. That lookup is the sanctioned mocking route CLAUDE.md names, not a leak of transport internals into response data; do not "fix" it by requiring a real API shape. `$rawTemplates` is a parameter rather than a second read from the registry inside the method: `translate()` already binds one snapshot for the rest of its body, and resolving the id against a different read's snapshot than the one it is then dereferenced against is exactly the kind of inconsistency a registry-backed hook invites. The method must genuinely return `null` (an ordinary raw response, or an `$error` with no matching `"httperror"` entry) for PHPStan L9 to accept the `?string` signature — a version that only ever returned a string is flagged `return.unusedType` and is not just a style nit, it is evidence the null branch was never reachable. Regression coverage: `tests/AbstractResponseTranslatorFallbackTest.php`, using a fixture registry rather than a real brand's — originally because the container was shared process-wide state (the RSRMID-2941 defect), now because what it needs is a registry *missing* an id every real brand ships.
  - **No enum or const set for template ids, on purpose.** The judgement predates RSRMID-2941 (which was then still going to replace the registry wholesale) and survives it: the id set is open by design — `addTemplate()` accepts any string, and that is the mocking route — so a closed type would have to be reopened by every caller registering one.
  - **Why this has no `*SeamTest.php` guard, unlike most seams in this file — and what is and is not actually caught.** (1) The parameter is compiler-enforced — PHP performs LSP checking on abstract-method implementations, so a brand `Client`/`Response` that drops the trailing `?string $error` from only *some* of `newResponse()`/`translate()` — an accidental, partial drift rather than a deliberate lockstep revert — is a fatal at declaration time, not a runtime drift a reflection test would need to catch. (2) **State plainly what is and is not caught, rather than overclaiming coverage.** A *partial* sentinel revert fails loudly: re-encoding `"httperror|"` onto `$raw` inside `post()` without also restoring `explode("|", $newraw, 2)` inside `translate()` leaves `$raw` matching no known template id, so `hasMissingRequiredFields()` routes to `"invalid"` (423) instead of `"httperror"` (421), and `tests/CNR/ClientTest.php::testRequestCurlExecFail2` — which asserts both `getCode() === 421` and the exact `getDescription()` string against the hand-authored `conn-error` cassette — fails on both axes. A *complete*, consistent revert (sentinel re-encoded, the `explode()` split restored, and `?string $error` dropped from all seven signatures together) compiles and is behaviour-preserving, so it would leave the suite green — that gap is **accepted as an out-of-scope risk, not closed by a guard**, because undoing the decision that way is a deliberate, whole-hierarchy reversal of a documented [MIGRATION.md → v30.0.0](../../MIGRATION.md#-v3000) `BREAKING CHANGE`, not the invisible one-line drift guard tests in this project exist to catch. (3) The only test shape left that would close even that remaining gap is a negative-space source sweep ("assert `"httperror|"` does not appear in `src/`"), which is exactly the vacuous-guard shape this file warns against elsewhere — it pins a string literal's absence, not a decision, and a rewrite using a different delimiter would slip through untouched.
  - **One plain behavioural test pins the newly-stated `TransportInterface` contract, because nothing else does.** `TransportSeamTest::testTransportErrorDiscardsParseableBytes()` injects a transport double returning real, parseable CNR bytes **and** a non-null error together, and asserts the `httperror` template wins — the bytes are discarded, not merged or preferred. This is ordinary behavioural coverage, not a guard: no reflection, no source sweep, just `request()` through the seam `TransportSeamTest` already exists to exercise (RSRMID-2910).
- **All three client-held collaborators are readable, so tests assert wiring through the interface instead of reflecting (RSRMID-2940, non-breaking).** The transport and logger seams were write-only — `newTransport()`/`setTransport()` and `newLogger()`/`setLogSink()`/`setCustomLogger()` with no reader — while the config seam had `getSocketConfig()`, whose docblock already credits the accessor with stopping forwarder sprawl. `AbstractClient::getTransport(): TransportInterface` and `getLogger(): LoggerInterface` close the asymmetry; there is nothing to remember about which of the three is readable.
  - **They are `@psalm-api` public API, not `@internal` — deliberately the opposite call from RSRMID-2938's three classes.** The test-reflection cleanup is the motivation, not the justification: the WHMCS Registrar Module embeds this SDK, installs its own logger via `setCustomLogger()` and injects canned API responses via `setTransport()`, and needs to assert both took effect from *outside* the package. `CommandRedactor` failed that test (nothing outside the SDK has a reason to call it); these pass it. Documented for consumers under README → "Testing your integration offline".
  - **Neither getter needs to appear on an interface, and `InterfaceCoverageSeamTest` will not object.** That sweep only examines concrete classes implementing one of its `TOTAL_INTERFACES`; no client implements any of them (`CNR\Client`'s `RoleCredentialsInterface` is deliberately excluded as an additive capability interface), so clients are outside its subject set entirely — `KNOWN_TOTAL_IMPLEMENTORS` is a floor of response/record/column/logger/transport classes. (Both counts this entry originally quoted have since moved — read the constants.) Client-only accessors are therefore legitimate, in the same way `getSocketConfig()` has always been.
  - **No guard test, and none is possible in the usual shape.** These accessors are not a seam whose removal is behaviour-preserving — delete one and every test calling it fails to compile. The reflection they replace is the thing being removed, so there is nothing structural left to pin.
  - **`LoggerSeamTest::testTheClientDefaultsToTheEchoSink()` was rewritten behaviourally rather than served by a third accessor.** It read `AbstractLogger::$sink` reflectively, which a *client*-side getter cannot reach; it now buffers output and asserts the default logger's record lands on standard output — which is the promise the shipped default actually makes, with the `EchoSink` identity being an implementation detail. **Rejected: `AbstractLogger::getSink()`.** `CNR\Logger`/`IBS\Logger` are concrete implementors of `LoggerInterface`, one of `InterfaceCoverageSeamTest`'s total contracts, so an inherited public `getSink()` is a stray-method failure; fixing that means declaring it on `LoggerInterface`, which is breaking — turning a no-bump ticket into a major to delete one reflection call.
  - **`SpyTransport` now records `$data` and `$closed`, closing two gaps its own docblock's promise had left open.** The spy recorded four of `post()`'s five arguments, so "the bytes on the wire are what `getPOSTData()` produced" was only ever asserted in halves — the encoding in `tests/CNR/ClientTest.php::testGetPostDataSecured()`, the delivery in `TransportSeamTest` — and a rewrite of either half alone kept both green. Proven by mutation: appending `"&MUTANT=1"` to `performRequest()`'s payload fails `testTheBytesOnTheWireAreWhatGetPostDataProduced()` **and nothing else in the suite**. Separately, no test called a *client's* `close()` — every `->close()` ran against a transport instance directly — so the one-line delegation in `AbstractClient::close()` was unexercised; emptying its body now fails `testClientCloseDelegatesToTheTransport()`.
- **The response-template registry is an instance, handed to the Response that needs it (RSRMID-2941, breaking, v32.0.0).** The container was `public static array $templates`, redeclared per brand and read live by the translator at translate time, so `addTemplate()` in one test class changed response translation in every later one. It is now `private array $templates` on an instantiable `ResponseTemplateManager`, seeded per instance from a per-brand `private const BUILTIN_TEMPLATES`, reached through the new `CNIC\ResponseTemplateManagerInterface`, and threaded `AbstractResponse::__construct(…, ?ResponseTemplateManagerInterface $templates = null)` → brand `translate()` → `AbstractResponseTranslator::translate()`. Every operation that was `static` is now an instance method; `resetTemplates()` and the `$builtinTemplates` per-class cache behind it are **deleted**, because state that cannot escape its object has nothing to reset.
  - **The seam stops at `AbstractResponse`; `AbstractClient` is untouched — a scope call, not an omission.** Every `addTemplate()` call site in the repo feeds a direct `new Response($templateId, …)`, never a `$client->request()`, so threading a registry through `performRequest()`/`newResponse()` and each brand override would add public surface for a capability nothing asks for. Adding it later is purely additive. **Revisit** on a real consumer need for bootstrap-time brand-wide overrides that every later `request()` sees — and answer it with the client-level thread, **not** by restoring the static container.
  - **`addTemplate()` returns `$this`, where the static predecessor returned `new static()`** — a throwaway instance of an all-static class, fluent in shape only. Chaining now registers onto the object the caller holds, which is the difference between `(new RTM())->addTemplate(…)->addTemplate(…)` working and quietly registering into nowhere.
  - **`getRawTemplates()` is separate from `getTemplates()`.** The translator needs the raw wire strings; `getTemplates()` builds a `Response` per entry, and building responses in order to translate one would recurse. The old code had the same split implicitly — the translator read the public property directly while `getTemplates()` constructed — which is precisely why the property could not be made private. Both were on the one interface until RSRMID-2968 put them on two, which is what turned "the translator must not call `getTemplates()`" from a comment into something the type system refuses.
  - **`matches()` now checks key existence before comparing, fixing the latent bug the ticket named.** `isTemplateMatchHash(["status" => "SUCCESS"], "404")` emitted `Undefined array key` and then compared `null`. **Trap when covering this:** the unguarded version still *returns* `false` (null never equals the template's value), so `assertFalse` alone is vacuous — and `.github/phpunit.xml` sets no `failOnWarning`, so the notice does not fail the build either. The guard installs a `set_error_handler` and asserts nothing was raised; without that, the mutation exits 0.
  - **`ResponseTemplateManagerInterface` is a "total" contract and joins `InterfaceCoverageSeamTest::TOTAL_INTERFACES` (RSRMID-2968 added `ResponseTemplateFactoryInterface` alongside it — read the constant rather than counting this sentence).** That guard's own revisit condition is "a genuinely new total interface is introduced", and this is one: it fully describes both brand managers, which until now implemented no swept interface and so had eight public methods the sweep could not see. Adding it was free — zero stray and zero widened methods on either brand. Verified non-vacuous the same way the guard's original entry was: a stray `public function strayMethod()` on `IBS\ResponseTemplateManager` now fails it, and did not before.
  - **Guard:** `tests/ResponseTemplateRegistrySeamTest.php`. Proven non-vacuous against four mutations, each failing it and nothing else: reintroducing any static property on a brand manager; a brand `translate()` that drops the `$templates` argument it was handed; a shared container behind the per-instance façade; and reverting the `matches()` existence check. The two `resetTemplates()` cases that used to close `tests/ResponseParserSeamTest.php` are gone with the API they described.

- **The registry publishes two disjoint contracts, and the Response-building hook takes only a template id (RSRMID-2968, breaking, v33.0.0).** The manager was two modules sharing one interface: a template store and a `Response` factory, eight public methods across four files against ~29 lines of behaviour, with `getRawTemplates()` as the only call site in `src/`. `ResponseTemplateManagerInterface` now carries the four registry methods (`generateTemplate()`, `addTemplate()`, `hasTemplate()`, `getRawTemplates()`) and the new `CNIC\ResponseTemplateFactoryInterface` carries the four Response-producing ones (`getTemplate()`, `getTemplates()`, `isTemplateMatchHash()`, `isTemplateMatchPlain()`). Per-instance ownership is untouched — that is RSRMID-2941's guarantee and it still holds.
  - **The two faces stay on one object; `AbstractResponseTemplateManager implements` both.** A standalone factory class was considered and rejected: building a brand `Response` requires brand knowledge (`createResponseFromTemplateId()`, `newResponseParser()`, `matchKeys()`), so a separate factory needs either a parallel abstract-plus-two-brands hierarchy — three new files, making the file count the ticket complained about worse — or a `templateFactory()` accessor on the registry contract, which hands the pipeline back the exact route the split exists to remove. Consumers opt in by *typing* against the face they need, which is what the ticket's "opt-in" buys; an opt-in **object** buys nothing further here.
  - **The narrowing is what closes the recursion, not a comment.** `translate()` holds a `ResponseTemplateManagerInterface`. While `getTemplate()` was declared on that type, one call from inside `translate()` built a `Response` whose constructor re-entered `translate()`; the only thing preventing it was the "don't use getTemplate as it leads to endless loop" comment at the call site. With the method off the type, that call is a static-analysis error.
  - **`createResponse(string $raw)` is `createResponseFromTemplateId(string $templateId)`, and `getTemplates()` builds from its own keys.** The old parameter carried two meanings: `getTemplate()` passed an id, `getTemplates()` passed wire text, and they agreed only because `resolveTemplateId()` substitutes a raw payload equal to a known id. Register a template whose wire text *is* another template's id and they diverged — `getTemplates()` handed back the other template. Both routes now address a template by id, so they cannot disagree. Note this keeps the mocking substitution rule intact (it is the sanctioned route, line 179 above); it stops the SDK's own factory from *depending* on it.
  - **`ResponseTemplateManagerInterface` is deliberately not a polymorphism seam, and the docblock now says so.** One direct implementer, the abstract class; both brands and the one test double inherit it, and the registry-seam guard's `translate()` assertion would pass unchanged if the parameter were typed to the abstract class instead. It is kept as an interface because it is the *narrowing point* — the thing that lets the pipeline hold a registry without reaching Response production — and because it documents what a third-party brand must satisfy. Do not re-justify it by claiming adapter-substitutability it does not have.
  - **Guard:** `tests/ResponseTemplateFactorySeamTest.php` (separate file, separate directive — the registry seam guards ownership, this one guards the split). Interface width is pinned by return type *and* by method name, because `getTemplates(): array` is an array of Responses that reflection sees as a plain `array`; the same name list is asserted **present** on the factory contract so deleting the four rather than moving them cannot make the pin vacuous. Proven non-vacuous against two mutations: putting `getTemplate()` back on `ResponseTemplateManagerInterface` (fails the shape half only), and reverting `getTemplates()` to iterate values (fails the divergence half only, with every other test in the suite still green — which is the whole argument for guarding it structurally).
