version: '3.9'

services:
  phpsdk:
    container_name: dstack-phpsdk
    volumes:
      - ..:/usr/share/rtldev-middleware-php-sdk:cached
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PHP_VERSION: 7.3
